# 2021.12.08
#etc/2021.12.08

---
```
신경세포(뉴런)와 학습

뉴런 모델링
(이전 뉴런과 연결)수상돌기-----축삭 돌기( 다음뉴런으로 전달)

입력->가중치->바이어스->활성함수->출력

xor 구현
- 단일 뉴런으로는 불가
	- XOR 문제는 뉴런 여러개로 다층의 망 구조로 구현 가능

다층 신경망
입력층과 출력층 사이에 1개 이상의 은닉층(hidden layer)을 가지는 신경망
- 은닉층이 많아 지면 학습의 정확도가 높아짐 -> 학습과정이 어려워짐
- 보통 2,3개 이상의 은닉층을 가지면 심층 신경망이라 함 -> 딥러닝


k층 가중치: W(53) -  이전층 노드 3에서 k층 노드 5로의 가중치 

오류 역전파
오차를 역으로 전달(역으로 가면서 계속 가중치 수정)

딥러닝(심층신경망)
- 은닉층의 개수가 점점 많아짐
제안 사항:
- 과적합->데이터셋이 더욱 더 많아야 한다.
- 컴퓨터도 훨씬 빨라야 한다
- 초기 가중치를 임의로 주면 어렵다
- 은닉층 층수와 노드수를 신중하게 계획
- 시그모이드 함수는 딥러닝에 맞지 않음
	- 은닉층이 많아지면 gradient vanishing

TensorFlow
- 데이터플로 그래프를 활용해 수치 계산(데이터 -> Tensor)
- 딥러닝과 머신러닝등에 활용하기위해 개발된 오픈소스 라이브러리

keras


머신러닝 방법과 전통적 통계분석 방법은 목적에서 차이가 발생

머신러닝 방법
- 예측의 성공 확률을 높이는 데에 목적이 있다
- 모델의 신뢰도나 정교한 가정은 중요성이 낮아지며,  여러 인자를 사용해 예측을 수행(오버피팅 어느정도 감안)

전통적 통계분석 방법
- 정해진 분포나 가정을 통해 실패 확률을 줄이는 데에 목적
- 모형의 복잡성보다는 단순성을 추구하며, 신뢰도가 중요
- 파라미터의 해석 가능성 또한 중요


오버피팅: 너무 세밀하게 학습 데이터나 하나하나를 다 설명하려다보니 정작 중요한 패턴을 설명할 수 없게되는 현상
오버피팅 해결방법
1. 데이터 자세히 살펴보기
2. 적절하게 수집된 데이터인지 확인 필요
3. 학습데이터 보강
4 학습데이터에 포함될 특성을 제한

데이터 전처리
- 모든 데이터 분석 프로젝트에서 반드시 거쳐야 하는 과정
- 분서가의 80% 시간을 데이터 수집 및 전처리에 사용(수집 20 전처리 60)

결측치 처리
이상치 처리
데이터 분포 변환
데이터 단위 변환

scikit-learn 전처리
-표준화
-정규화

pandas

Dataframe 생성
Dictionary, List
read_csv
describe 함수
.loc(인덱스 or 컬럼 명 사용)
.iloc 함수 (인덱스 번호 사용)

random 라이브러리

pandas에서 DataFrame에 적용되는 함수들
...

상관계수/공분산(확률변수 2개의 상관정도) 구하기
...

정렬하기
...


Merge
merge 함수는 두 데이터 프레임의 공통 열 혹은 인덱스를 기준으로 두개의 테이블을 합친다(기본 inner join)
outerjoin 방식은 키가 한쪽에만 있어도 데이터를 보여줌 

concat
데이터연결(기본 axis=0)
axis =1 열기준으로 연결

groupby함수
그룹별로 분류
.unstack 행/열로 풀어서 보여줌
.swap_level, .sort_index
.filter함수 - 원하는 data만추출

시각화 라이브러리
빅데이터 분석 시각화의 필요성
matplotlib
fill_between
타이틀 설정(title)
막대 그래프 그리기(bar)
산점도 그리기(scatter)
```